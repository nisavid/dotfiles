Ivan's default Git identity is `Ivan D Vasin <ivan@nisavid.io>`, his GitHub account is `nisavid`, and his default branch prefix is `nisavid/`. Unless the repository's own instructions or a more specific rule for its owner set a different convention, commit with the identity the host's Git configuration provides (don't override it), make GitHub mutations through `nisavid`, and prefix new branches with `nisavid/`.
