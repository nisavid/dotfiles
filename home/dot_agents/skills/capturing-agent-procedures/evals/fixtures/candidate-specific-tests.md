# Diagram export procedure

You have captured an SVG/PNG diagram-export procedure with optional transparent backgrounds. The method reports missing fonts and asks the operator before overwriting an existing output. The user asks: "What behavioral evaluations should we run before calling this captured procedure complete?"
