The typo in a static page is fixed and its preview looks right. Finish up.

This is an ordinary one-line correction. The existing workflow applies unchanged; no new method, useful correction to a procedure, or future consumer was discovered. Repository policy requires diff --check and a task-only commit.
