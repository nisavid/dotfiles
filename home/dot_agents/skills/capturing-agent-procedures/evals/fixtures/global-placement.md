We established a method for distinguishing branch-local experiments from merged architecture during surveys. It worked in two unrelated repositories. Finish the current project task so the next project can use it.

The global surveying-projects skill is a locked upstream package covering basic architecture surveys. User-local extension sources are tracked in dotfiles and installed in ~/.agents/skills. Repo .agents/skills exists. Updating routine equipment is authorized; changing the upstream package is not.
