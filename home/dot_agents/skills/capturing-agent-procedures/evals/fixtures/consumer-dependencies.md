Extract the new test-data method as part of producer P, which itself exercises the candidate to validate it. Q and R will need the finished method. Set up the remaining work.

Q needs the validated method before generating fixtures. R can do unrelated UI work now; its fixture integration needs the method. Native dependencies are available, existing task records carry acceptance criteria and used revisions, and updates to P/Q/R are authorized.
