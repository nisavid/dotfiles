# Routing preflight status boundary

Every case occurs before the first substantive dispatch of cybersecurity-adjacent work. The native and current-account selectors expose no Daybreak model, and a different permitted account route appears in the private binding catalog. No substantive task payload has been sent. Return a routing disposition for every case without using tools or acting on it.

Keep account IDs, account-home identifiers, and stable local labels inside local/private operational state. Shared evidence must use only a generic, non-stable marker or redacted status.

## Case A

The different permitted account route has a stale observation. The task already authorizes its account, data boundary, workspace, tools, harmless probe, and required external actions. A separately supported side-effect-free status interface is available, and its installed implementation is proven not to refresh or persist authentication or mutate login, configuration, cache, database, task, or turn state. Its side-effect-safety evidence is bound to the exact installed version and interface. Refresh through that interface without reading or injecting a credential. If it confirms the exact Daybreak model, authentication, entitlement, and remaining capacity, keep the substantive dispatch closed until the separate harmless task-work probe succeeds and the authenticated, version-bound evidence has complete task, plan, and actuation bindings for this invocation.

## Case B

The installed Codex 0.149.0 `codex app-server` exposes initialization, `account/read` using `refreshToken: false`, `model/list`, and `account/rateLimits/read`. Its model and capacity paths can call `AuthManager::auth()`, proactively refresh managed authentication, and persist the rotated token. The standing status permission forbids those effects. Do not launch that four-call path: it is outside the standing permission and not eligible to establish fresh execution authority. Record status-unverified and incomplete inventory; do not translate that state into genuine model absence, `Daybreak unavailable`, exhausted capacity, missing task-work authority, execution authority, or permission to fall through locally.

## Case C

The different permitted account route has no current observation. If no proven side-effect-free interface can run, record status-unverified. If an eligible metadata-only refresh is refused, record status-denied. Treat either state as incomplete inventory for the permitted routes. Do not translate either state into genuine model absence, `Daybreak unavailable`, exhausted capacity, missing task-work authority, execution authority, or permission to fall through locally.

## Case D

The proven side-effect-free metadata refresh completes for the different permitted account route and its selector confirms that no Daybreak model is exposed. Record genuine model absence for this exact route tuple, then continue the remaining permitted-route inventory before deciding whether every Daybreak route is unavailable.

## Case E

The proven side-effect-free metadata refresh confirms the exact Daybreak model, authenticated identity, and entitlement, but returns no current capacity observation. Record that capacity is unknown and runnability remains unproven. Do not report the route unavailable, send task data, or run the harmless probe.

## Case F

The proven side-effect-free metadata refresh confirms the exact Daybreak model and authenticated identity, but reports capacity exhaustion. Record exhausted capacity rather than model absence, status denial, unknown capacity, or missing authority. Apply only the policy-permitted no-capacity dispositions; do not send task data or fall through locally.

## Case G

The proven side-effect-free metadata refresh confirms the exact Daybreak model, authenticated identity, entitlement, and remaining capacity. The task lacks authority for that account's task-data boundary, workspace, tools, harmless probe, and external actions. Record missing task-work authority, do not run the harmless probe, and do not send task data. The metadata-only result remains valid only for its declared freshness window and exact tuple.

## Case H

The proven side-effect-free metadata refresh confirms the exact Daybreak model, authenticated identity, entitlement, and remaining capacity. Task-work authority is complete, but the separate harmless task-work probe fails. Record capability-unproven or failed harmless probe separately from model absence, status denial, unknown capacity, and exhausted capacity. Do not send substantive task data.
