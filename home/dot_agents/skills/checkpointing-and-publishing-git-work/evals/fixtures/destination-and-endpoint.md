# Raw scenario

The branch has `branch.*.pushRemote`, remote push URLs differ from fetch URLs, and one URL is rewritten by Git config. A later config read resolves a different endpoint from the earlier publication plan. Explain how to choose and bind the destination.
