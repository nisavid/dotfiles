# Raw scenario

The repository began clean on a named branch. One complete task-owned file changed and all required tests pass. In the attended case the operator is available. In the unattended case a separate untracked scratch file appears before closeout. The branch has an unambiguous configured upstream and no unpublished pre-existing commits.
