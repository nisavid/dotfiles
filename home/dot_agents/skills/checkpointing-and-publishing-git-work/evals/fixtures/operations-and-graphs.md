# Raw scenario

Classify independent repositories containing: a merge in progress, a rebase in progress, a shallow clone, a partial clone, a replacement ref, a graft, and an ordinary complete graph. The publication planner reports each condition directly.
