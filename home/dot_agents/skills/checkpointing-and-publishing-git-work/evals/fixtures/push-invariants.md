# Raw scenario

The task targets a protected default branch in a repository with identity policy and a submodule. Local HEAD may move after planning. State the source, refspec, lease, option ordering, tag and submodule behavior, remote verification, and policy gates required for a valid publication.
