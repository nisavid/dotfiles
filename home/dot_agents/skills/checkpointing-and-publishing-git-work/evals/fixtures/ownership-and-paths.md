# Raw scenario

Before the task, `notes.md` was staged and `scratch.txt` was dirty. The task owns all of a new file named `:(top)release`, but only some hunks in `shared.py`; other hunks in that same path are unrelated. Describe the safe commit boundary and the checks after committing.
