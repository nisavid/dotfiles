# Raw scenario

A named branch has no chosen completion route, so merge, PR, keep, or discard is genuinely unresolved. Separately, a completed task is at detached HEAD. Assign the completion choice, push, PR creation, merge/discard/cleanup, and detached-state actions to the appropriate workflow.
