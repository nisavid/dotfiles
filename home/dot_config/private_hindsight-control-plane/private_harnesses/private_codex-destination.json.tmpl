{{- $hindsight := .hindsight -}}
{{- if not (get $hindsight "publicFixture") -}}
{{- $private := include ".private-hindsight.toml.age" | decrypt | fromToml -}}
{{- $hindsight = $private.hindsight -}}
{{- end -}}
{
  "schema_version": 1,
  "harness_id": "codex",
  "hooks_path": "{{ joinPath .chezmoi.homeDir ".codex/hooks.json" }}",
  "settings_path": "{{ joinPath .chezmoi.homeDir $hindsight.personalCodexHome "settings.json" }}",
  "tools_path": "{{ joinPath .chezmoi.homeDir $hindsight.stateRoot "harness-tools/codex.json" }}",
  "rollback_root": "{{ joinPath .chezmoi.homeDir $hindsight.stateRoot "harness-rollbacks" }}"
}
