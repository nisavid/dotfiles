# vim: ft=sh
# shellcheck shell=sh disable=SC2034

# Legacy Podman command paths remain available for unmigrated workloads.
# OrbStack is the default for new Hindsight and general container work.
PATH=/opt/podman/bin:$PATH
